"""鸣潮 —— 本文件夹**只装这个游戏的生成着色栈**,没有面板,也没有 ``GAME_MODULE``。

面板那一半由引擎家族模块画(见 ``Game/SBUE``):一份解码器读遍这台引擎的每个发行版,
所以一个模块画遍它们的 Scene / Characters,鸣潮不另起一套 —— 那会是同一件事的第二套系统。

着色不是这样:**每个发行版有自己的一套**。所以栈按游戏走,一个游戏一个文件夹,名字就是这个
发行版自报的产品名(装机探针报什么,这里就叫什么),生成器按同名的目标把产物写进来。
``Game`` 的注册按文件夹认栈、不跟着模块走,于是「有栈没模块」是正常形态,不是漏了什么。
"""

from __future__ import annotations
