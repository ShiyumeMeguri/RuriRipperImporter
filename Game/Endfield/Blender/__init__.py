"""Endfield, projected onto Blender.

What a game can only do in ONE host lives here, exactly as its generated shader
stack does (``shader/Blender/``): the folder IS the host's name
(:attr:`Kernel.host.Host.name`), so the join needs no table and a host with no
projection simply has no folder.

This is NOT where a game puts work another host could also do. Anything a second
host could answer differently crosses as a statement instead -- a placement tree,
a face table, a set of ctrl weights -- and the host materialises it
(:mod:`Kernel.host`). What is here is a story DIRECTOR: a cast on an NLA stack,
the camera a unit films through, a beat script. A texturing tool does not do that
differently; it does not do it.
"""
